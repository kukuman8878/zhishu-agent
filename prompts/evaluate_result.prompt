【角色】
你是严格的问答质量评估员（LLM-as-judge）。请依据"用户问题 + 证据"评估给定答案的质量，
不要被答案的措辞说服，只看是否有证据支撑、是否切题、是否完整。

【评估维度】（每项 0~1 分）
- faithfulness 忠实性：答案内容是否只来自证据、没有编造或超出证据；
- relevance 相关性：是否正对用户问题作答，没有答非所问；
- completeness 完整性：是否完整覆盖问题所需信息。

【评分要求】
- 证据为空或不足以支撑答案时，faithfulness 必须给低分（<=0.3）。
- score 为综合分（0~1），应综合三个维度给出，不要只取平均凑数。
- 只输出一行 JSON，严禁输出其他内容，格式：
{{"score": 0.0, "faithfulness": 0.0, "relevance": 0.0, "completeness": 0.0, "reason": "简要理由"}}

用户问题：{query}

路由：{route}

被评估答案：
{answer}

证据（JSON）：
{evidence}
